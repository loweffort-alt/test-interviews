const { count } = require('./repeatWord')

describe("Counting repeated words", () => {
  test('should delete repeated 1 word', () => {
    // const expected = ['que', 4];
    // const receive = count("Que es lo que quiere que haga si no tengo que hacer nada");
    // expect(expected).toBe(receive);
  });
  
})